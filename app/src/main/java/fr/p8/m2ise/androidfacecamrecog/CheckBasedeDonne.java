package fr.p8.m2ise.androidfacecamrecog;

import android.app.Activity;

/**
 * Created by zhaleh on 16/11/23.
 */

public class CheckBasedeDonne extends Activity {
    /**boolean checkDataBase() {
     PostsDatabaseHelper checkDB = null;
     try {
     checkDB = PostsDatabaseHelper.getInstance(, null, PostsDatabaseHelper.OPEN_READONLY);
     checkDB.close();
     } catch (PostsDatabaseHelper e) {
     // base de données n'existe pas.
     }
     return checkDB != null ? true : false;
     }**/
}
